const BAD_REQUEST = 400;
const NOT_FOUND = 404;
const SERVER_ERROR = 500;

module.exports = {
  SERVER_ERROR_MESSAGE: "An error has occurred on the server",
  BAD_REQUEST,
  NOT_FOUND,
  SERVER_ERROR,
};
